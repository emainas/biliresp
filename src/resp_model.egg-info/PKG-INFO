Metadata-Version: 2.4
Name: resp-model
Version: 0.1.0
Summary: Tools for parsing RESP output and ESP grid data
Author: E. Mainas
Requires-Python: >=3.9
Description-Content-Type: text/markdown

# resp-model

Utilities for parsing RESP output (`resp.out`) and ESP grid (`esp.xyz`) files, with
supporting CLI and tests.

## Installation

```bash
python -m pip install -e .
```

## Usage

```python
from parsers import ParseRespDotOut

parser = ParseRespDotOut("data/resp.out", 78)
esp_frames, resp_frames = parser.extract_frames_with_esp()
esp_grid_frames = parser.extract_esp_from_espxyz("data/esp.xyz")
```

Run the CLI helper:

```bash
python scripts/check_resp.py
```

Run the tests:

```bash
pytest
```

